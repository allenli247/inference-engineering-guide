[project]
name = "inference-engineering-guide"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.8.5",
    "nbformat>=5.10.4",
    "numpy>=2.4.6",
    "plotly>=6.8.0",
    "torch>=2.12.0",
    "transformers>=5.9.0",
    "triton>=3.7.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
